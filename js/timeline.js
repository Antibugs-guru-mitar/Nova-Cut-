import{state}from"./state.js";import{$,fmt}from"./utils.js";export function renderTimeline(select){let t=$("#videoTrack");t.innerHTML="";if(!state.clips.length){t.innerHTML='<div class="timeline-empty">Import media to build your timeline</div>';return}state.clips.forEach(c=>{let e=document.createElement("div");e.className="clip"+(c.id===state.selectedClipId?" selected":"");e.innerHTML=`<div class="clip-name">${c.sourceName}</div><div class="clip-duration">${fmt((c.end-c.start)/(c.speed||1))}</div>`;e.onclick=()=>select(c.id);t.appendChild(e)})}