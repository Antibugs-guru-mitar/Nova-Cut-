import{state,snapshot,restore}from"./state.js";export function checkpoint(){state.history.push(snapshot());if(state.history.length>40)state.history.shift();state.future=[]}export function undo(){if(!state.history.length)return false;state.future.push(snapshot());restore(state.history.pop());return true}export function redo(){if(!state.future.length)return false;state.history.push(snapshot());restore(state.future.pop());return true}